package EasyProblems;

public class PairCubeCount {
	
	static int solution(int n){
		int length = (int)(Math.cbrt(n));
		int l =1;
		int result = 0;
		while(l < length) {
			int r =l+1;
			if(Math.pow(l, 3) + Math.pow(r, 3) == n) {
				result+= 2;
				l=r;
			}else {
				r++;
			}
		}
		return result;
	}
   public static void main(String[] args) {
	System.out.println(solution(35));
}
}
